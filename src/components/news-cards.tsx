import { ArrowUpRight, Radio } from "lucide-react";
import Link from "next/link";
import type { NewsItem } from "@/lib/feed-parser";
export function NewsCards({ items, compact = false }: { items: NewsItem[]; compact?: boolean }) {
  if (!items.length) return <div className="news-empty"><div className="news-empty-icon"><Radio size={27} /></div><div><h3>O radar está se preparando.</h3><p>As novidades chegam aqui depois de uma revisão humana. Enquanto isso, explore as fontes oficiais.</p><div className="source-links"><a href="https://openai.com/news/" target="_blank" rel="noreferrer">OpenAI <ArrowUpRight size={13}/></a><a href="https://www.anthropic.com/news" target="_blank" rel="noreferrer">Anthropic <ArrowUpRight size={13}/></a><a href="https://blog.google/technology/ai/" target="_blank" rel="noreferrer">Google AI <ArrowUpRight size={13}/></a></div></div></div>;
  return <div className="news-grid">{items.slice(0, compact ? 3 : 100).map(item => <article key={item.id} className="news-card"><div className="news-card-top"><span className="source-symbol">{item.source === "OpenAI" ? "◎" : "✦"}</span><span>{item.source}</span><time dateTime={item.published_at}>{new Date(item.published_at).toLocaleDateString("pt-BR", { day: "2-digit", month: "short", year: "numeric", timeZone: "UTC" })}</time></div><h3><a href={item.url} target="_blank" rel="noreferrer">{item.title}</a></h3><div className="news-card-bottom"><span>Na fonte original</span><a href={item.url} target="_blank" rel="noreferrer" aria-label={`Ler na fonte: ${item.title}`}><ArrowUpRight size={19} /></a></div></article>)}</div>;
}
export function RadarHeading() { return <div className="section-heading radar-heading"><h2>No radar da IA.</h2><p>Acompanhe o que muda nas ferramentas que você está aprendendo a usar.</p><Link href="/noticias" className="text-link">Ver todas as notícias <ArrowUpRight size={17}/></Link></div>; }
